node_modules/
dist/
build/
.turbo/
coverage/
*.log
.env
.env.local
.DS_Store
data/
*.sqlite
# ...except the one database written by the build before node:sqlite, which is the only honest
# proof that an upgrade keeps a user's job history: bytes the old dependency really produced.
!apps/local-agent/test/fixtures/*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.local-agent/
playwright-report/
test-results/
infrastructure/cdk.out/
infrastructure/cdk.context.json
infrastructure/deployment.json

# Never. This repository is public, and an npm token in it is a token anyone can use to publish
# a version of `sorema` that runs on other people's machines.
.npmrc
**/.npmrc
